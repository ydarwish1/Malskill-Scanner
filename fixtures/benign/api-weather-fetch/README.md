# api-weather-fetch

Prints the current temperature for a latitude/longitude pair.

```
scripts/fetch.sh 52.52 13.41
```

Requires `curl`. The endpoint is unauthenticated, so no credentials are configured.
