# context-saver

Saves and restores a working set of editor tab titles. Nothing leaves the project
directory.

The `test/` and `tests/` directories contain injection-hardening cases whose fixtures
are, by necessity, literal dangerous commands. That is what makes this bundle a trap for
the scanner: the strings are present, and none of them is behaviour.
